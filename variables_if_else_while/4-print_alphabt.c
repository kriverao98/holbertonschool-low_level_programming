#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0(Success)
 */

int main(void)
{
	char az = 'a';
{
	for (az = 'a'; az <= 'z'; az++)
	{
		if (az != 'q' && az != 'e')
			putchar(1);
	}
	{
		putchar('\n');
	}
}
	return (0);
}

#include "main.h"

/**
 * mul - Function that multiplies two ints.
 * @a - Variable to be multiplied.
 * @b - Variable to be multiplied.
 * Return: Void (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}

#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - program that prints the alphabet
 * Return: Void (Success)
 */

void print_alphabet(void)
{
	char a;
	{
		for (a = 'a'; a <= 'z'; a++)
		putchar(a);
		{
			putchar('\n');
		}
	}
}

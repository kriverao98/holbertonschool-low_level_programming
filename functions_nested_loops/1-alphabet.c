#include <stdio.h>
#include <main.h>

/**
 * entry point - simple program that prints the alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)
{
	print_alphabet();
	
	return (0);
}

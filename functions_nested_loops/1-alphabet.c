#include <stdio.h>
#include "main.h"

/**
 * void print_alphabet(void) - program that prints the alphabet
 * Return: Void (Success)
 */

void print_alphabet(void)
{
	char a;
	{
		for (a = 'a'; a <= 'z'; a++)
		putchar(a);
		{
			putchar('\n');
		}
	}
}

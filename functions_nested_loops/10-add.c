#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two ints and returns the result
 * @a: first num to be computed
 * @b: second num to be computed
 * Return: a, b (Success)
 */

int add(int a, int b)
{
	return (a + b);
}

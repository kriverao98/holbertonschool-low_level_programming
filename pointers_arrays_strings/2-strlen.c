#include "main.h"
#include <string.h>
/**
 * _strlen - This function returns the length of a string.
 * @s: Variable to be checked
 * Return: Lenght of string
 */

int _strlen(char *s)
{
	int str;

	str = strlen(s);

	return (str);
}
